# Quote Gui Application

You can store & read quotes.

## Installation

Prepare files for compilation:

```
$ cmake . -B build/ 
```

Compile binary:

```
$ cmake --build build/
```

## Status

Developed & tested on `Debian 11 bullseye (GUI)`.

Should be compatible with MacOS & Windows.

